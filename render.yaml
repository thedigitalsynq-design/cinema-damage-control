services:
  - type: web
    name: cinema-damage-control
    runtime: node
    buildCommand: npm ci && npm run build
    startCommand: node server.js
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20.19.0
